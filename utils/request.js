/**
 * {
 *   url<string>
 *   header<Object>
 *   method<string> defaut GET
 *   dataType<string>
 * }
 */
const request = opts => {
  return new Promise((resolve, reject) => {
    wx.request({

    })
  });
}